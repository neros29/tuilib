 there will be miltiple layers of hte api the rendering will just be the lowest level but above that we have moer layers for instce there will be such a thing calld elements that are simply virtual surfaces that can be put on a surface however these elements can be given style propertys and so on but are only virtual since they will not acutly be indivle surfaces just rendered on to the main surfacewich are renderd on to the screen surface and then on to the terrminal. this allows for elements with diffrent proertys like wether it can be selected or not wich can change the way it looks when selected or it can change what key binds do when selected like an element can have it's own key binds. I will order things in the way were there is a list of priority in key binds this is what deceds the hiarchy of actions when the same key is used for miltiple things the hiarchy will go from top down meaning the elemnts are the highst level however a key bind can also be givn the level of -1 wich will mean nothing can over write it this is used for instce if you want a quit action that works anyware these should be defined with in the main apllication layer the tui can be thouhg of in this way the application this is the lowest level it can handle key binds have basic elemtns however is mostly built up of widgets each widget on the application is built of elements. each widget has a surface as well as it's own widget level keybinds each element is built with it's own keybinds and a virtual surface that is compiled to the main surface before runtime this virual suface is mostly there to make the creation of widgets simler. this is how the application will work.