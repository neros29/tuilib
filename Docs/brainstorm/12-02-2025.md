I think to make this simpler as simple is how you acutly get things done i need to make surface have a function to add a raw charcter vectory to the screen I can then use this fact to add labels to the screen or what every i want to the screen for instece i could creae a lable by using going through eaceh letter in the string for the label appending a charecter to the charecter vector with the current letter as well sa fg and bg. I mean realy i should probly I should have the virutal surface be a struct or class as it will need to be able to hold z axis as well as being able to change it depending on the style for instce you could have a style sheet that says if selcted make hte bg diffrent and by having each element it's own struct or class it can have dirty flags and the likes i mean with overload operaters i could make the = operater for instce when your saying `v_surf[index] = char` or what every you it could then togole the dirty flag on or it could littery just cheak that specific cordanites it would take me understading operaters better but i think this could workd. basicly surface allows you to add virtual surfaces to it each virtual surface can be modifed directly thorough its array and has a dirty flag system to tell surface wether it needs to render it's self again also i think widgets should be sort of put aside for know instead going for a simpler idea sort of less tui and more just terminal rendering and input I should be able to build some pertty good tui's with this layout just in a more low level type way wich to be honst is fine for most things i plan to build and kind of what i am going for