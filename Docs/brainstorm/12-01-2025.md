I am going to need to make sure that everything is accesed changed and so on through seters so that i can use this for optimizatoin and dirty flaging however i would like to have surfaces changed throught the surface refrence since the surfaces will be just a bunch of elements i also think it might make sence to simply get rid of the surface class entirly and just make the screen be what is used i just 
I would like my api to be user frinedly and use the desgine principle that performance is handeled by the api this means no matter how badly the user's code is it dosent affect the acutle rendering performance. I would also like the user to have as low level contorl as possable this means the ablity to change every indvidle charecter on the screen if they so wish however having many layers of abstraction as well allowsing users to creat useful abstract widgets of there own this means widgets will be a data type that the user can use to construct there own widgets as well as the data type that I will use to build the standerd widgets. I would like the flow of the code to be that the user simply initlizes the screen as done in pygame this creats the main screen and this is what all outher surfaces are appended to each indivlle surface will be identical in data to the screen allowing for user to append surfaces to each indivle surface I will try to have as much optimization and cheaking to make absutly sure that as much as possable of this is done at the start of the program or only when the screen changes but basicly the everything will be built of surfaces this means a surface is a genaric data type responsable for holding an array of charecters and responsable for allowing the user to append diffrent virtual surfaces on to the surface a virtual surface will just be a surface array that can be manupilated by the user however a virtual surface can not be added to the the screen and will alwas be cast on to the surface it is appended to at start time of the program not for each frame, this means so far we have our screen object this is what everythign works off of this is intilized and is what acutly renders each frame next we have our surface object wich simply is responsable for at load time casting each elemnt it is assoiated with on to an array and then being append to the screen to then be cast on teh screen during run time then we also have the elments or virtual surfaces these are simply surfaces in there rawest form being just a vector of charecter objects as well as a few outher variables but hte surface is compltey open to the public this is how a user can manipulate per charecter on the display also each eleme. acutly to be honst i think i havent quite figuerd out how i want to do this as it is 