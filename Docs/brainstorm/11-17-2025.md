My idea is to first simpy creat a sort of sub api this is the api that allows for the building of widgets and the main section of my tui app this will include the rendering the input handleing and so on and will provied an elagant way to crate widgets surfaces get user input and render everthing to the terrminl. 