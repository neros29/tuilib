### Input Class
This class will using raii put the terminal in raw mode in the constructed and out of raw mode in the destructer. There is then a function for the user to get a single char called get_char
- method get_char returns a single charecter returned
- private method is_char tells get char if there is a char in cin
