Weird surface offset indexing leading to when offset is below 2 labeles renderd with an offset less then 2 the first letter is missing.
Label tends to not allow for deleting charecters as label happens to just renrender each time on top of the old render instead of clearing first.
